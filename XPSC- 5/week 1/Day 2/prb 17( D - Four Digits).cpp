#include<bits/stdc++.h>
using namespace std;
#define int long long
#define all(x) (x).begin(), (x).end()
#define rall(x) (x).rbegin(), (x).rend()
#define FAST ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#define no cout<<"NO"<<'\n'
#define yes cout<<"YES"<<'\n'
#define endl '\n'
typedef vector<int> vi;
typedef pair<int, int> pii;

void solve(){
    // string s;
    // cin >> s;
    // while(s.length()<4)
    //     s = '0' + s;
    // cout << s << endl;
    int n;
    cin >> n;
    cout << setfill('0') << setw(4) << n << endl;
    //setfill('0'): Sets the padding character to `'0'` for any extra spaces.
    //setw(4): Sets the minimum width of the output to 4 characters.
}

int32_t main(){
    FAST
    int t=1;
    // cin>>t;
    while(t--) solve();
    return 0;
}